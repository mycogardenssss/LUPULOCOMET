This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.env.local.example
.gitignore
next.config.js
nul
package.json
pages/api/salvar.js
pages/index.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="nul">

</file>

<file path=".env.local.example">
GOOGLE_PROJECT_ID=
GOOGLE_PRIVATE_KEY_ID=
GOOGLE_PRIVATE_KEY=
GOOGLE_CLIENT_EMAIL=
GOOGLE_CLIENT_ID=
GOOGLE_SHEETS_ID=
</file>

<file path="package.json">
{
  "name": "lupulo-pra-fumar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^14.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "googleapis": "^140.0.1"
  }
}
</file>

<file path="pages/api/salvar.js">
import { google } from 'googleapis';

export default async function handler(req, res) {
  if (req.method !== 'POST') {
    res.setHeader('Allow', 'POST');
    return res.status(405).json({ success: false, error: 'Method not allowed' });
  }

  const { nome, telefone, instagram } = req.body || {};

  if (!nome || !telefone || !instagram) {
    return res.status(400).json({ success: false, error: 'Nome, telefone e Instagram são obrigatórios' });
  }

  try {
    const auth = new google.auth.GoogleAuth({
      credentials: {
        type: 'service_account',
        project_id: process.env.GOOGLE_PROJECT_ID,
        private_key_id: process.env.GOOGLE_PRIVATE_KEY_ID,
        private_key: process.env.GOOGLE_PRIVATE_KEY?.replace(/\\n/g, '\n'),
        client_email: process.env.GOOGLE_CLIENT_EMAIL,
        client_id: process.env.GOOGLE_CLIENT_ID,
        auth_uri: 'https://accounts.google.com/o/oauth2/auth',
        token_uri: 'https://oauth2.googleapis.com/token',
        auth_provider_x509_cert_url: 'https://www.googleapis.com/oauth2/v1/certs',
        client_x509_cert_url: `https://www.googleapis.com/robot/v1/metadata/x509/${process.env.GOOGLE_CLIENT_EMAIL}`,
      },
      scopes: ['https://www.googleapis.com/auth/spreadsheets'],
    });

    const sheets = google.sheets({ version: 'v4', auth });

    const timestamp = new Date().toLocaleString('pt-BR', {
      timeZone: 'America/Sao_Paulo',
    });

    await sheets.spreadsheets.values.append({
      spreadsheetId: process.env.GOOGLE_SHEETS_ID,
      range: 'Página1!A:D',
      valueInputOption: 'USER_ENTERED',
      requestBody: {
        values: [[nome, telefone, instagram, timestamp]],
      },
    });

    return res.status(200).json({ success: true });
  } catch (error) {
    console.error('Google Sheets error:', error);
    return res.status(500).json({
      success: false,
      error: error.message || 'Erro ao salvar no Google Sheets',
    });
  }
}
</file>

<file path=".gitignore">
node_modules/
.next/
.env.local
</file>

<file path="next.config.js">
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
</file>

<file path="pages/index.js">
import Head from 'next/head';
import { useState } from 'react';

const maskPhone = (value) => {
  const digits = value.replace(/\D/g, '');
  if (digits.length <= 2) return '(' + digits;
  if (digits.length <= 3) return '(' + digits.slice(0, 2) + ') ' + digits.slice(2);
  if (digits.length <= 7) return '(' + digits.slice(0, 2) + ') ' + digits.slice(2, 3) + ' ' + digits.slice(3);
  if (digits.length <= 11) return '(' + digits.slice(0, 2) + ') ' + digits.slice(2, 3) + ' ' + digits.slice(3, 7) + '-' + digits.slice(7);
  return '(' + digits.slice(0, 2) + ') ' + digits.slice(2, 3) + ' ' + digits.slice(3, 7) + '-' + digits.slice(7, 11);
};

export default function Home() {
  const [nome, setNome] = useState('');
  const [telefone, setTelefone] = useState('');
  const [instagram, setInstagram] = useState('');
  const [message, setMessage] = useState({ text: '', type: '' });
  const [loading, setLoading] = useState(false);

  const clearMsg = () => setMessage({ text: '', type: '' });

  const validate = () => {
    const n = nome.trim();
    const t = telefone.replace(/\D/g, '');
    const ig = instagram.trim();

    if (!n) {
      setMessage({ text: 'Coloca seu nome aí 🌿', type: 'error' });
      return false;
    }

    if (t.length < 10) {
      setMessage({ text: 'Telefone incompleto — bota o DDD também', type: 'error' });
      return false;
    }

    if (!/^@([A-Za-z0-9_.]){2,}$/.test(ig)) {
      setMessage({ text: 'Coloca seu @ do Instagram correto', type: 'error' });
      return false;
    }

    return true;
  };

  const handleSubmit = async () => {
    clearMsg();
    if (!validate()) return;

    setLoading(true);

    const payload = {
      nome: nome.trim(),
      telefone: telefone.trim(),
      instagram: instagram.trim(),
    };

    try {
      const res = await fetch('/api/salvar', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      });

      if (!res.ok) {
        let errorText = res.statusText || 'Erro de servidor';
        try {
          const body = await res.text();
          if (body) errorText = body;
        } catch (e) {
          console.error('Erro ao ler corpo de erro:', e);
        }
        setMessage({ text: `Erro de servidor: ${res.status} — ${errorText}`, type: 'error' });
        setLoading(false);
        return;
      }

      const data = await res.json();
      if (data && data.success) {
        setMessage({ text: 'Show! Recebi seu Instagram e já te chamamos 🌿', type: 'success' });
        setNome('');
        setTelefone('');
        setInstagram('');
      } else {
        setMessage({ text: (data && data.error) || 'Algo deu errado, tenta de novo.', type: 'error' });
      }
    } catch (e) {
      console.error('Fetch error:', e);
      setMessage({ text: 'Sem conexão. Tenta de novo em alguns segundos.', type: 'error' });
    } finally {
      setLoading(false);
    }
  };

  return (
    <>
      <Head>
        <title>Lúpulo pra Fumar — Amostra Grátis</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&family=Space+Mono:wght@400;700&display=swap"
          rel="stylesheet"
        />
      </Head>

      <main>
        <header className="hero">
          <svg className="logo-svg" viewBox="0 0 72 72" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <circle cx="36" cy="36" r="34" stroke="#7FD44C" strokeWidth="1.5" strokeDasharray="4 3" />
            <ellipse cx="36" cy="36" rx="14" ry="20" fill="none" stroke="#7FD44C" strokeWidth="1.5" />
            <line x1="36" y1="16" x2="36" y2="56" stroke="#7FD44C" strokeWidth="1" />
            <ellipse cx="36" cy="28" rx="8" ry="5" fill="none" stroke="#7FD44C" strokeWidth="1" />
            <ellipse cx="36" cy="36" rx="11" ry="6" fill="none" stroke="#7FD44C" strokeWidth="1" />
            <ellipse cx="36" cy="44" rx="8" ry="5" fill="none" stroke="#7FD44C" strokeWidth="1" />
            <line x1="36" y1="14" x2="46" y2="6" stroke="#D4AF37" strokeWidth="1.5" strokeLinecap="round" />
          </svg>

          <div className="event-tag">trap & afins: anomaly</div>

          <h1>
            LÚPULO<br />
            <span>PRA FUMAR.</span>
          </h1>

          <p className="hero-sub">Sem nicotina. Com terpenos e sinergia com a flor.</p>

          <a href="#comparativo" className="scroll-hint" aria-label="Rolar para conteúdo">
            ↓ entende por quê antes de pegar sua amostra ↓
          </a>
        </header>

        <div className="divider" />

        <section id="comparativo">
          <p className="section-label">Por dentro</p>
          <h2>Lúpulo vs. Tabaco</h2>

          <div className="table-wrap">
            <table className="compare-table">
              <thead>
                <tr>
                  <th>Característica</th>
                  <th>Lúpulo</th>
                  <th>Tabaco</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>Nicotina</td>
                  <td><span className="tag-yes">✓ nenhuma</span></td>
                  <td><span className="tag-no">alta concentração</span></td>
                </tr>
                <tr>
                  <td>Dependência química</td>
                  <td><span className="tag-yes">✓ muito baixa</span></td>
                  <td><span className="tag-no">vicia rápido</span></td>
                </tr>
                <tr>
                  <td>Terpenos benéficos</td>
                  <td><span className="tag-yes">✓ mirceno, humuleno…</span></td>
                  <td><span className="tag-no">praticamente zero</span></td>
                </tr>
                <tr>
                  <td>Sinergia com cannabis</td>
                  <td><span className="tag-yes">✓ potencializa entourage</span></td>
                  <td><span className="tag-no">suprime o THC</span></td>
                </tr>
                <tr>
                  <td>Auxílio no sono</td>
                  <td><span className="tag-yes">✓ sedativo natural</span></td>
                  <td><span className="tag-no">estimulante</span></td>
                </tr>
                <tr>
                  <td>Sabor</td>
                  <td><span className="tag-yes">✓ floral, amargo, persistente</span></td>
                  <td><span className="tag-no">alcatrão</span></td>
                </tr>
                <tr>
                  <td>Preserva sabor da flor</td>
                  <td><span className="tag-yes">✓ realça e prolonga</span></td>
                  <td><span className="tag-no">mascara tudo</span></td>
                </tr>
                <tr>
                  <td>Aditivos químicos</td>
                  <td><span className="tag-yes">✓ natural, zero aditivos</span></td>
                  <td><span className="tag-no">dezenas de aditivos</span></td>
                </tr>
                <tr>
                  <td>Anti-inflamatório</td>
                  <td><span className="tag-yes">✓ humuleno + cariofileno</span></td>
                  <td><span className="tag-no">agressor respiratório</span></td>
                </tr>
              </tbody>
            </table>
          </div>
        </section>

        <div className="divider" />

        <section id="terpenos">
          <p className="section-label">O que tem dentro</p>
          <h2>Os terpenos</h2>

          <div className="terpene-grid">
            <div className="terpene-card">
              <span className="terpene-icon" aria-hidden="true">🫧</span>
              <p className="terpene-name">Mirceno</p>
              <p className="terpene-desc">O mais abundante. Sedativo natural, relaxante muscular. Presente na manga e nas melhores indica. Amplifica o efeito relaxante quando combinado com cannabis.</p>
            </div>
            <div className="terpene-card">
              <span className="terpene-icon" aria-hidden="true">🌿</span>
              <p className="terpene-name">Humuleno</p>
              <p className="terpene-desc">O terpeno "exclusivo" do lúpulo — o nome vem de <em>Humulus lupulus</em>. Anti-inflamatório potente, antibacteriano. Aroma herbal intenso.</p>
            </div>
            <div className="terpene-card">
              <span className="terpene-icon" aria-hidden="true">💜</span>
              <p className="terpene-name">Linalol</p>
              <p className="terpene-desc">O mesmo terpeno da lavanda. Ansiolítico, relaxante, auxílio no sono. Suaviza a fumaça e dá um toque floral. Potencializa o CBN da cannabis.</p>
            </div>
            <div className="terpene-card">
              <span className="terpene-icon" aria-hidden="true">🔥</span>
              <p className="terpene-name">Beta-Cariofileno</p>
              <p className="terpene-desc">Único terpeno que age como canabinoide — ativa receptores CB2. Anti-inflamatório sistêmico. Efeito entourage real, não só aromático.</p>
            </div>
          </div>
        </section>

        <div className="divider" />

        <section id="sinergia">
          <p className="section-label">A combinação</p>
          <h2>Sinergia com a flor</h2>

          <div className="sinergia-block">
            <h3>🌸 Por que misturar?</h3>
            <p className="sinergia-text">Lúpulo e cannabis são da mesma família botânica (Cannabaceae) e compartilham terpenos. Quando misturados:</p>
            <ul className="sinergia-list">
              <li>Gosto da flor fica mais forte e dura mais na boca</li>
              <li>Efeito entourage amplificado por terpenos complementares</li>
              <li>Queima mais uniforme e lenta</li>
              <li>Sem nicotina suprimindo o THC</li>
              <li>Volume maior sem perder qualidade da experiência</li>
            </ul>
            <p className="sinergia-note">É uma mistura inteligente, não um mero "esticador".</p>
          </div>
        </section>

        <div className="divider" />

        <div className="form-section" id="amostra">
          <div className="form-wrapper">
            <h2 className="form-headline">Pega sua amostra <span className="accent">grátis</span></h2>
            <p className="form-sub">Tô levando amostras pro evento.<br />Preenche aqui, te chamo no WhatsApp.</p>

            <div className="form-group">
              <label htmlFor="nome">Nome completo</label>
              <input
                type="text"
                id="nome"
                name="nome"
                placeholder="Seu nome"
                autoComplete="name"
                required
                value={nome}
                onChange={(event) => {
                  setNome(event.target.value);
                  clearMsg();
                }}
              />
            </div>

            <div className="form-group">
              <label htmlFor="telefone">WhatsApp</label>
              <input
                type="tel"
                id="telefone"
                name="telefone"
                placeholder="(11) 9 0000-0000"
                autoComplete="tel"
                required
                value={telefone}
                onChange={(event) => {
                  setTelefone(maskPhone(event.target.value));
                  clearMsg();
                }}
              />
            </div>

            <div className="form-group">
              <label htmlFor="instagram">Instagram</label>
              <input
                type="text"
                id="instagram"
                name="instagram"
                placeholder="@seuusuario"
                autoComplete="username"
                required
                value={instagram}
                onChange={(event) => {
                  setInstagram(event.target.value);
                  clearMsg();
                }}
              />
            </div>

            <button type="button" className="btn-cta" onClick={handleSubmit} disabled={loading}>
              <span className="btn-text" style={{ display: loading ? 'none' : 'flex' }}>QUERO MINHA AMOSTRA 🌿</span>
              {loading && <span className="spinner" aria-hidden="true" />}
            </button>

            <p className="form-fine">Entrega pessoal. Sem enrolação.</p>

            <div className={`form-msg ${message.type}`} role="alert" aria-live="polite" style={{ display: message.text ? 'block' : 'none' }}>
              {message.text}
            </div>
          </div>
        </div>

        <footer>
          <p>Lúpulo para fumar · produto natural · sem nicotina · sem aditivos</p>
        </footer>
      </main>

      <style jsx global>{`
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        :root {
          --bg: #080C08;
          --surface: #111811;
          --surface2: #182018;
          --accent: #7FD44C;
          --accent2: #C8FF6B;
          --gold: #D4AF37;
          --purple: #9D6FFF;
          --text: #EEEEE0;
          --muted: #778877;
          --border: #1E2A1E;
        }
        html { scroll-behavior: smooth; }
        body { background: var(--bg); color: var(--text); font-family: 'Space Grotesk', sans-serif; font-size: 16px; line-height: 1.6; overflow-x: hidden; }
        .mono { font-family: 'Space Mono', monospace; }
        .accent { color: var(--accent); }
        .gold { color: var(--gold); }
        .purple { color: var(--purple); }
        .muted { color: var(--muted); }
        .hero { min-height: 100svh; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 4rem 1.5rem 3rem; position: relative; overflow: hidden; }
        .hero::before { content: ''; position: absolute; inset: 0; background: radial-gradient(ellipse 70% 50% at 50% 30%, rgba(127,212,76,0.07) 0%, transparent 70%); pointer-events: none; }
        .logo-svg { width: 72px; height: 72px; margin-bottom: 1.5rem; animation: spin-slow 12s linear infinite; }
        @keyframes spin-slow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
        .event-tag { font-family: 'Space Mono', monospace; font-size: 11px; letter-spacing: 0.2em; color: var(--gold); text-transform: uppercase; margin-bottom: 2rem; border: 1px solid rgba(212,175,55,0.25); padding: 6px 18px; border-radius: 100px; }
        .hero h1 { font-size: clamp(2.6rem, 10vw, 5rem); font-weight: 700; line-height: 1.05; letter-spacing: -0.03em; margin-bottom: 1.25rem; }
        .hero h1 span { color: var(--accent); }
        .hero-sub { font-size: clamp(1rem, 3vw, 1.2rem); color: var(--muted); max-width: 420px; margin-bottom: 2.5rem; }
        .scroll-hint { font-family: 'Space Mono', monospace; font-size: 11px; letter-spacing: 0.15em; color: var(--muted); text-decoration: none; display: inline-block; animation: bounce 2s ease-in-out infinite; cursor: pointer; }
        @keyframes bounce { 0%,100% { transform: translateY(0); } 50% { transform: translateY(6px); } }
        section { padding: 5rem 1.5rem; max-width: 720px; margin: 0 auto; }
        .section-label { font-family: 'Space Mono', monospace; font-size: 10px; letter-spacing: 0.25em; color: var(--accent); text-transform: uppercase; margin-bottom: 1.25rem; }
        section h2 { font-size: clamp(1.6rem, 5vw, 2.2rem); font-weight: 700; letter-spacing: -0.02em; margin-bottom: 2rem; line-height: 1.2; }
        .divider { width: 100%; max-width: 720px; margin: 0 auto; height: 1px; background: var(--border); }
        .compare-table { width: 100%; border-collapse: collapse; font-size: 0.92rem; }
        .compare-table th { font-family: 'Space Mono', monospace; font-size: 10px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--muted); padding: 0.75rem 1rem; border-bottom: 1px solid var(--border); text-align: left; }
        .compare-table th:nth-child(2) { color: var(--accent); }
        .compare-table th:nth-child(3) { color: var(--muted); }
        .compare-table td { padding: 0.7rem 1rem; border-bottom: 1px solid rgba(30,42,30,0.6); color: var(--text); vertical-align: middle; }
        .compare-table td:first-child { color: var(--muted); font-size: 0.85rem; }
        .compare-table tr:hover td { background: rgba(127,212,76,0.03); }
        .tag-yes { display: inline-flex; align-items: center; gap: 5px; color: var(--accent); font-size: 0.85rem; }
        .tag-no { display: inline-flex; align-items: center; gap: 5px; color: var(--muted); font-size: 0.85rem; text-decoration: line-through; text-decoration-color: rgba(119,136,119,0.4); }
        .terpene-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1rem; }
        .terpene-card { background: var(--surface); border: 1px solid var(--border); border-radius: 16px; padding: 1.5rem 1.25rem; position: relative; overflow: hidden; transition: border-color 0.2s; }
        .terpene-card:hover { border-color: rgba(127,212,76,0.3); }
        .terpene-icon { font-size: 1.8rem; margin-bottom: 0.75rem; display: block; }
        .terpene-name { font-family: 'Space Mono', monospace; font-size: 11px; letter-spacing: 0.2em; text-transform: uppercase; margin-bottom: 0.5rem; }
        .terpene-card:nth-child(1) .terpene-name { color: var(--accent); }
        .terpene-card:nth-child(2) .terpene-name { color: var(--accent); }
        .terpene-card:nth-child(3) .terpene-name { color: var(--purple); }
        .terpene-card:nth-child(4) .terpene-name { color: #FF8C42; }
        .terpene-desc { font-size: 0.85rem; color: var(--muted); line-height: 1.55; }
        .sinergia-block { background: var(--surface); border: 1px solid var(--border); border-left: 3px solid var(--accent); border-radius: 0 16px 16px 0; padding: 2rem 1.75rem; }
        .sinergia-block h3 { font-size: 1.1rem; font-weight: 700; color: var(--accent); margin-bottom: 1rem; font-family: 'Space Mono', monospace; letter-spacing: 0.1em; }
        .sinergia-list { list-style: none; display: flex; flex-direction: column; gap: 0.65rem; }
        .sinergia-list li { font-size: 0.95rem; color: var(--text); padding-left: 1.25rem; position: relative; }
        .sinergia-list li::before { content: '→'; position: absolute; left: 0; color: var(--accent); font-family: 'Space Mono', monospace; }
        .sinergia-note { margin-top: 1.25rem; font-size: 0.85rem; color: var(--muted); font-style: italic; }
        .sinergia-text { color: var(--muted); font-size: 0.9rem; margin-bottom: 1.25rem; }
        .form-section { padding: 5rem 1.5rem 6rem; }
        .form-wrapper { max-width: 480px; margin: 0 auto; background: var(--surface); border: 1px solid var(--border); border-radius: 24px; padding: 2.5rem 2rem; position: relative; overflow: hidden; }
        .form-wrapper::before { content: ''; position: absolute; top: -1px; left: 50%; transform: translateX(-1%); width: 60%; height: 2px; background: var(--accent); border-radius: 0 0 4px 4px; }
        .form-headline { font-size: 1.5rem; font-weight: 700; text-align: center; margin-bottom: 0.5rem; letter-spacing: -0.02em; }
        .form-sub { text-align: center; color: var(--muted); font-size: 0.9rem; margin-bottom: 2rem; line-height: 1.5; }
        .form-group { margin-bottom: 1rem; }
        .form-group label { display: block; font-size: 11px; font-family: 'Space Mono', monospace; letter-spacing: 0.15em; text-transform: uppercase; color: var(--muted); margin-bottom: 0.4rem; }
        .form-group input { width: 100%; background: var(--bg); border: 1px solid var(--border); border-radius: 10px; padding: 0.85rem 1rem; color: var(--text); font-size: 1rem; font-family: 'Space Grotesk', sans-serif; transition: border-color 0.2s; outline: none; -webkit-appearance: none; }
        .form-group input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(127,212,76,0.12); }
        .form-group input::placeholder { color: var(--muted); opacity: 0.6; }
        .btn-cta { width: 100%; margin-top: 0.75rem; padding: 1rem; background: var(--accent); color: #080C08; font-family: 'Space Grotesk', sans-serif; font-size: 1rem; font-weight: 700; border: none; border-radius: 10px; cursor: pointer; letter-spacing: 0.03em; transition: background 0.15s, transform 0.1s; display: flex; align-items: center; justify-content: center; gap: 8px; }
        .btn-cta:hover:not(:disabled) { background: var(--accent2); }
        .btn-cta:active:not(:disabled) { transform: scale(0.98); }
        .btn-cta:disabled { opacity: 0.6; cursor: not-allowed; }
        .form-fine { text-align: center; margin-top: 1rem; font-size: 0.8rem; color: var(--muted); }
        .form-msg { margin-top: 1rem; padding: 0.85rem 1rem; border-radius: 10px; font-size: 0.9rem; text-align: center; }
        .form-msg.success { background: rgba(127,212,76,0.1); border: 1px solid rgba(127,212,76,0.3); color: var(--accent); }
        .form-msg.error { background: rgba(220,60,60,0.08); border: 1px solid rgba(220,60,60,0.25); color: #FF6B6B; }
        footer { text-align: center; padding: 2rem 1.5rem; font-size: 0.8rem; color: var(--muted); border-top: 1px solid var(--border); }
        @media (max-width: 500px) { .terpene-grid { grid-template-columns: 1fr; } .compare-table { font-size: 0.82rem; } .compare-table td, .compare-table th { padding: 0.6rem 0.6rem; } .form-wrapper { padding: 2rem 1.25rem; } }
        @media (prefers-reduced-motion: reduce) { .logo-svg, .scroll-hint { animation: none; } }
        .spinner { width: 16px; height: 16px; border: 2px solid rgba(8,12,8,0.3); border-top-color: #080C08; border-radius: 50%; animation: spin 0.7s linear infinite; }
        @keyframes spin { to { transform: rotate(360deg); } }
        .table-wrap { overflow-x: auto; -webkit-overflow-scrolling: touch; }
      `}</style>
    </>
  );
}
</file>

</files>
